import { Association } from '../resources/Enums';
export default function associationDefaults(associations) {
    return Object.entries(associations).reduce((obj, [k, v]) => {
        if (v == Association.HasMany) {
            obj[k] = [];
        }
        return obj;
    }, {});
}
