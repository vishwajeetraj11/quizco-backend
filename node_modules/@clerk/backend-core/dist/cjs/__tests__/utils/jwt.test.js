"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const jwt_1 = require("../../util/jwt");
test('check jwt claims with no issuer', () => {
    const dummyClaims = {
        sub: 'subject',
        exp: 1643374283,
    };
    expect(() => (0, jwt_1.checkClaims)(dummyClaims)).toThrow(`Issuer is invalid: ${dummyClaims.iss}`);
});
test('check jwt claims with invalid issuer', () => {
    const dummyClaims = {
        sub: 'subject',
        exp: 1643374283,
        iss: 'invalid-issuer',
    };
    expect(() => (0, jwt_1.checkClaims)(dummyClaims)).toThrow(`Issuer is invalid: ${dummyClaims.iss}`);
});
test('check jwt claims with valid issuer', () => {
    const dummyClaims = {
        sub: 'subject',
        exp: 1643374283,
        iss: 'https://clerk.happy.path',
    };
    expect(() => (0, jwt_1.checkClaims)(dummyClaims)).not.toThrow();
});
test('check jwt claims with invalid azp', () => {
    const dummyClaims = {
        sub: 'subject',
        exp: 1643374283,
        iss: 'https://clerk.happy.path',
        azp: 'invalid-azp',
    };
    const authorizedParties = ['valid-azp', 'another-valid-azp'];
    expect(() => (0, jwt_1.checkClaims)(dummyClaims, authorizedParties)).toThrow(`Authorized party is invalid: ${dummyClaims.azp}`);
});
test('check jwt claims with no azp and no authorized parties', () => {
    const dummyClaims = {
        sub: 'subject',
        exp: 1643374283,
        iss: 'https://clerk.happy.path',
    };
    expect(() => (0, jwt_1.checkClaims)(dummyClaims)).not.toThrow();
});
test('check jwt claims with no azp and provided authorized parties', () => {
    const dummyClaims = {
        sub: 'subject',
        exp: 1643374283,
        iss: 'https://clerk.happy.path',
    };
    const authorizedParties = ['valid-azp', 'another-valid-azp'];
    expect(() => (0, jwt_1.checkClaims)(dummyClaims, authorizedParties)).not.toThrow();
});
test('check jwt claims with azp and no authorized parties', () => {
    const dummyClaims = {
        sub: 'subject',
        exp: 1643374283,
        iss: 'https://clerk.happy.path',
        azp: 'random-azp',
    };
    expect(() => (0, jwt_1.checkClaims)(dummyClaims)).not.toThrow();
});
test('check jwt claims with no azp and provided authorized parties', () => {
    const dummyClaims = {
        sub: 'subject',
        exp: 1643374283,
        iss: 'https://clerk.happy.path',
        azp: 'valid-azp',
    };
    const authorizedParties = ['a-valid-azp', 'valid-azp', 'another-valid-azp'];
    expect(() => (0, jwt_1.checkClaims)(dummyClaims, authorizedParties)).not.toThrow();
});
